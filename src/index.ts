/**
 * Programmatic entry point re-exporting the library's public API.
 * The CLI (see cli/) is a thin wrapper around these same modules.
 */

export * from './detect';
export * from './context';
export * from './generate';
export * from './verify';
